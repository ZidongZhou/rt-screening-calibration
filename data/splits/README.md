# Participant splits

Participant split identifier CSVs are not stored in this repository. Regenerate them after downloading the source CSV files:

```bash
python scripts/03_create_splits.py
```

The aggregate outputs needed for package checks are under `results/`.
