# Metadata note

Question word counts used by the response-time residualization model are stored as small constants in `src/rt_screening/engine.py`. The old metadata CSV files are not redistributed because they duplicated dataset-derived files and contained encoding artifacts.
